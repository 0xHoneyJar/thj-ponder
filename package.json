{
  "name": "thj-ponder",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "ponder dev",
    "start": "rm -rf .ponder && node server.js",
    "start:direct": "ponder start",
    "codegen": "ponder codegen",
    "reset": "rm -rf .ponder && echo 'Cleared Ponder cache. Database must be reset manually.'",
    "reset:hard": "rm -rf .ponder .ponder-cache node_modules/.ponder && echo 'Cleared all Ponder caches. Database must be reset manually.'",
    "reset:db": "node scripts/reset-and-reindex.js",
    "reset:all": "npm run reset:hard && npm run reset:db"
  },
  "dependencies": {
    "@ponder/core": "^0.6.4",
    "hono": "^4.9.2",
    "viem": "^2.21.0",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  }
}